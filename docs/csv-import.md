# CSV import

_Filled in with Phase 12._

Covers: the supported columns per post type, the field-mapping step, validation and the per-row
error report, resuming a partial import, and the export format.
